
#include <iostream>
#include <cstring>

using namespace std;

int main()
{
	string strInput = "";
	cout<<"plz input: ";
	cin>>strInput;

	int length = strInput.length();
	cout<<strInput<<endl;
	
	char chInput[length] = {};
	for(int i = 0; i < length; ++i)
	{
		if(strInput[i])
	}

	return 0;
}
